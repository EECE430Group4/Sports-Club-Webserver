drop table if exists users;
drop table if exists players;
drop table if exists article;
CREATE TABLE users (
  name TEXT NOT NULL,
  username TEXT PRIMARY KEY,
  password TEXT NOT NULL,
  role TEXT NOT NULL
  );
  
  CREATE TABLE players (
  playerid INTEGER PRIMARY KEY,
  name TEXT NOT NULL,
  age INTEGER NOT NULL,
  team TEXT NOT NULL,
  position TEXT NOT NULL,
  points INTEGER,
  assists INTEGER
);


CREATE TABLE article (
  articleid int PRIMARY KEY,
  articletitle text NOT NULL,
  articleheadline text NOT NULL,
  articlebody text NOT NULL
);

CREATE TABLE shop (
  rownum int NOT NULL,
	shopitemid	text PRIMARY KEY,
	sizeSstock	int NOT NULL,
	sizeMstock	int NOT NULL,
	sizeLstock	int NOT NULL,
	itemprice	int NOT NULL,
	itemName	text NOT NULL
);
  
INSERT INTO shop VALUES (1,'S1',10,10,10,20,'T-shirt'), (2,'S2',10,10,10,20,'Shorts'), (3,'S3',10,10,10,20,'Ball'), (4,'S4',10,10,10,20,'Hoodie'), (5,'S5',10,10,10,20,'Long-sleeve'), (6,'S6',10,10,10,20,'T-shirt black'), (7,'S7',10,10,10,20,'T-shirt'), (8,'S8',10,10,10,20,'T-shirt');
  
  
create table posts (
  username TEXT NOT NULL,
  dateposted DATE NOT NULL,
  body TEXT NOT NULL,
  FOREIGN KEY (username)
  REFERENCES users (username),
  PRIMARY KEY(username,dateposted)
);

CREATE TABLE games (
  ID INTEGER primary key AUTOINCREMENT,
  sport TEXT not null,
  club1 text not null,
  score text not null,
  club2 text not null,
  gamedate date not null
)







INSERT INTO users VALUES (1,'Fan ElFan','fan1','123','FAN'),(2,'Player ElPlayer','player1','123','PLAYER'),(3,'Admin ElAdmin','admin1','123','ADMIN'),(4,'hassan khatib','ekha','123','FAN');
